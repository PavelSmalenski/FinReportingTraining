namespace Entities.Reports.Rpt1WeeklyBalance;

public record struct WeeklyBalanceControlDates(
    DateTime Date1,
    DateTime Date2,
    DateTime Date3,
    DateTime Date4,
    DateTime Date5,
    DateTime Date6,
    DateTime Date7
);